{
  "rules": {
    "sessions": {
      "$code": {
        ".read": "auth != null && data.child('created').val() > now - 86400000",
        "created": {
          ".read": "auth != null",
          ".write": "auth != null && !data.exists()",
          ".validate": "newData.isNumber() && newData.val() <= now && newData.val() > now - 60000"
        },
        "creatorUid": {
          ".write": "auth != null && !data.exists()",
          ".validate": "newData.isString() && newData.val() === auth.uid"
        },
        "slots": {
          "0": {
            ".write": "auth != null && (!data.exists() || !root.child('sessions').child($code).child('participants').child('0').exists())",
            ".validate": "newData.isString() && newData.val() === auth.uid"
          },
          "1": {
            ".write": "auth != null && (!data.exists() || !root.child('sessions').child($code).child('participants').child('1').exists())",
            ".validate": "newData.isString() && newData.val() === auth.uid"
          },
          "2": {
            ".write": "auth != null && (!data.exists() || !root.child('sessions').child($code).child('participants').child('2').exists())",
            ".validate": "newData.isString() && newData.val() === auth.uid"
          },
          "3": {
            ".write": "auth != null && (!data.exists() || !root.child('sessions').child($code).child('participants').child('3').exists())",
            ".validate": "newData.isString() && newData.val() === auth.uid"
          },
          "4": {
            ".write": "auth != null && (!data.exists() || !root.child('sessions').child($code).child('participants').child('4').exists())",
            ".validate": "newData.isString() && newData.val() === auth.uid"
          },
          "$other": { ".validate": false }
        },
        "participants": {
          "$slot": {
            ".write": "auth != null && root.child('sessions').child($code).child('slots').child($slot).val() === auth.uid && (!newData.exists() || !data.exists() || (newData.child('lat').val() === data.child('lat').val() && newData.child('lng').val() === data.child('lng').val()) || newData.child('ts').val() > data.child('ts').val() + 2000)",
            ".validate": "newData.hasChildren(['uid', 'lat', 'lng', 'accuracy', 'ts'])",
            "uid": {
              ".validate": "newData.isString() && newData.val() === auth.uid"
            },
            "lat": {
              ".validate": "newData.isNumber() && newData.val() >= -90 && newData.val() <= 90"
            },
            "lng": {
              ".validate": "newData.isNumber() && newData.val() >= -180 && newData.val() <= 180"
            },
            "accuracy": {
              ".validate": "newData.isNumber() && newData.val() >= 0"
            },
            "ts": { ".validate": "newData.isNumber()" },
            "name": {
              ".validate": "newData.isString() && newData.val().length <= 30"
            },
            "$other": { ".validate": false }
          }
        },
        "$other": { ".validate": false }
      }
    }
  }
}
